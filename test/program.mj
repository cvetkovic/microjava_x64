program TestProgram

    int globalInt;
    char globalChar;

    {
        void main()
            int a, b, c, d, e, f, g, h, i, j;
            int x, y, z;
            int arr[];
        {
            // array
            x = arr[i];
            arr[j] = y;
            x = arr[i];
            y = arr[i];
        }
    }