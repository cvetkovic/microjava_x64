program Program    {        int test(int a, int b, int c)            int x;        {            //print(x + 2);            //print(b);            return a + b + c;        }        void main()            int x, a, b, c, d;            int niz[];        {            //niz = new int[x + 1];            //a = x + 1;            //a = niz[x + 3];            niz[x + 1] = a;            //test(x + 1, a + 2);            //read(b);            //print(x + b);            /*a = b * (-c) + b * (-c);            a = b * c + b * c;            x = a + a * (b - c) + (b - c) * d;            x = x + 10;*/        }    }