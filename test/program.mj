program Program    {        int test(int a, int b, int c)            int x;        {            /*print(a);            print(b);            print(c);            print(x);*/            return a + b + c;        }        void main()            int x, a, b, c, d;        {            //read(b);            //print(x);            //a = b * (-c) + b * (-c);            //a = b * c + b * c;            x = a + a * (b - c) + (b - c) * d;            x = x + 10;        }    }