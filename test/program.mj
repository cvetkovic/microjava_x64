program TestProgram

    int globalInt;
    char globalChar;

    class Klasa
    {
        int polje;
        {
            int metodaKlase(int a, int b, int c)
            {
                return 1;
            }
        }
    }

    Klasa k;

    {
        int SaKlasom(Klasa k, int kx, int ky)
        {
            return 1;
        }

        int BezKlase(int k, int kx, int ky)
        {
            return 1;
        }

        void main()
            int a, b, c, d, e, f, g, h, i, j;
            int x, y, z;
            int arr[];
        {
            //k = new Klasa;

            // common subexpression elimination -> array
            //x = arr[i];
            //arr[j] = y;
            //x = arr[i];
            //y = arr[i];

            a = k.polje;
            BezKlase(1, 2, 3);
            b = k.polje;
            SaKlasom(k, 1, 2);  // this should delete the DAG
            c = k.polje;

            k.metodaKlase(4, 5, 6);

            k.polje = b;
            k.polje = c;
            k.polje = a;
        }
    }