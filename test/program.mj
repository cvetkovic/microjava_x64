program TestProgram

    int globalInt;
    char globalChar;

    {
        void main()
            int a, b, c, d, e, f, g, h, i, j;
            int x, y, z;
            int arr[];
        {
            // dead code
            d = b + a;
            d = f + e;

            // array
            //x = arr[i];
            //arr[j] = y;
            //z = arr[i];
            //x = arr[i];
        }
    }