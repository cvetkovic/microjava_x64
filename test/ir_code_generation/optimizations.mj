program TestProgram
    {
        void main()
            int a, b, c, d, e, f, g;
        {
            // ---- CSE
            //a = b * c;
            //d = b * c * e;
            //f = b * c * a;

            // constant folding
            //a = 2 * 3 + b;
            //a = 2 * 3 - 4 + b;

            // algebraic identities
            b = a + 0;
            b = a - 0;
            b = a - a;
            b = 2 * a;
            b = a * 1;
            b = a * 0;
            b = a / 1;
            b = a / a;

            // ---- CSE
            //a = d;
            //b = d;
            //c = d;
            //d = e;

            // ---- CSE
            //a = b + c;
            //b = b - d;
            //c = c + d;
            //e = b + c;

            // ---- CSE
            //a = b + c;
            //d = 5;
            //b = b + c;
            //d = b + c;

            // ---- identity
            //a = a;
        }
    }