program MJProgram
{
	void main()
	    int i, a, b, c;
	{
	    for (i = 0; i < 10; i++)
	    {
	        c = 0;      // isn't invariant (CRIT 1 & 2)

	        if (i % 2 == 1)
	            c = 1;  // isn't invariant (CRIT 1)

            // no invariant code here
	        print(c);
	        print(eol);
	    }
	}
}

